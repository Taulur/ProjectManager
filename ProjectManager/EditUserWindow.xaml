<Window x:Class="ProjectManager.EditUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjectManager"
        mc:Ignorable="d"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
xmlns:val="clr-namespace:ProjectManager.ValidationRules"
        Title="Взаимодействие с пользователем" Height="550" Width="350" ResizeMode="NoResize" Icon="/account-group.ico">
    <Grid>
        
        <Border HorizontalAlignment="Center" VerticalAlignment="Center" Background="White" Style="{StaticResource ShadowBorder}"  >
            <StackPanel>
                <Border Padding="15" CornerRadius="15" Margin="10" Background="White" Style="{StaticResource ShadowBorder}">
                    <TextBlock  FontSize="15" FontWeight="Bold" HorizontalAlignment="Center" x:Name="PopupTitle" Text="Взаимодействие с пользователями"/>
                </Border>
                <StackPanel>
                    <TextBlock Margin="5" FontSize="12" FontWeight="SemiBold" Text="📁 Username пользователя"/>
                    <Border Style="{StaticResource ShadowBorder}">
                        <TextBox Margin="5"
materialDesign:HintAssist.Hint="Username"
materialDesign:HintAssist.IsFloating="True"
materialDesign:ValidationAssist.UsePopup="False"
materialDesign:ValidationAssist.OnlyShowOnFocus="True">
                            <TextBox.Text>
                                <Binding Path="User.Username" UpdateSourceTrigger="PropertyChanged">
                                    <Binding.ValidationRules>
                                        <val:LoginValidation/>
                                    </Binding.ValidationRules>
                                </Binding>
                            </TextBox.Text>
                        </TextBox>
                    </Border>
                </StackPanel>
                <StackPanel>
                    <TextBlock Margin="5" FontSize="12" FontWeight="SemiBold" Text="📋 ФИО пользователя"/>
                    <Border Style="{StaticResource ShadowBorder}">
                        <TextBox Margin="5"
materialDesign:HintAssist.Hint="Фамилия Имя Отчество"
materialDesign:HintAssist.IsFloating="True"
materialDesign:ValidationAssist.UsePopup="False"
materialDesign:ValidationAssist.OnlyShowOnFocus="True">
                            <TextBox.Text>
                                <Binding Path="User.Fullname" UpdateSourceTrigger="PropertyChanged">
                                    <Binding.ValidationRules>
                                        <val:FullnameValidation/>
                                    </Binding.ValidationRules>
                                </Binding>
                            </TextBox.Text>
                        </TextBox>
                    </Border>
                </StackPanel>
                <StackPanel>
                    <TextBlock Margin="5" FontSize="12" FontWeight="SemiBold" Text="📋 Пароль"/>
                    <Border Style="{StaticResource ShadowBorder}">
                        <PasswordBox Margin="5" materialDesign:HintAssist.Hint="Пароль" materialDesign:HintAssist.IsFloating="True" materialDesign:ValidationAssist.UsePopup="False"
                                     materialDesign:PasswordBoxAssist.IsPasswordRevealed="True" materialDesign:ValidationAssist.OnlyShowOnFocus="True">
                            <materialDesign:PasswordBoxAssist.Password>
                                <Binding Path="User.Password" UpdateSourceTrigger="PropertyChanged">
                                    <Binding.ValidationRules>
                                        <val:PasswordValidation />
                                    </Binding.ValidationRules>
                                </Binding>
                            </materialDesign:PasswordBoxAssist.Password>
                        </PasswordBox>
                    </Border>
                </StackPanel>
                <StackPanel>
                    <TextBlock Margin="5" FontSize="12" FontWeight="SemiBold" Text="📋 Подтверждение пароля"/>
                    <Border Style="{StaticResource ShadowBorder}">
                        <PasswordBox Margin="5"
materialDesign:HintAssist.Hint="Повторите пароль"
materialDesign:HintAssist.IsFloating="True"
materialDesign:ValidationAssist.UsePopup="False"
materialDesign:ValidationAssist.OnlyShowOnFocus="True">
                            <materialDesign:PasswordBoxAssist.Password>
                                <Binding Path="ConfirmPassword" UpdateSourceTrigger="PropertyChanged">
                                    <Binding.ValidationRules>
                                        <val:PasswordValidation />
                                    </Binding.ValidationRules>
                                </Binding>
                            </materialDesign:PasswordBoxAssist.Password>
                        </PasswordBox>
                    </Border>
                </StackPanel>
                <StackPanel Margin="5" Grid.Column="0" Orientation="Vertical">
                    <TextBlock HorizontalAlignment="Center" Text="Роль"></TextBlock>
                    <ComboBox Text="{Binding User.Systemrole.Title}" ItemsSource="{Binding DbService.SystemRoles}"
DisplayMemberPath="Title"
SelectedValuePath="Id"
materialDesign:HintAssist.Hint="Выберите роль"
SelectedValue="{Binding SelectedRoleId}"
IsEditable="True" Margin="0,5" IsReadOnly="True"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Margin="5" Click="Execute" Foreground="White" Height="35" FontWeight="SemiBold" Background="Green" Content="Выполнить"></Button>
                    <Button Margin="5" Click="Back" Foreground="White" Height="35" FontWeight="SemiBold" Background="DarkRed" Content="Вернуться"></Button>
                </StackPanel>
                
            </StackPanel>
        </Border>
        <materialDesign:Snackbar x:Name="MainSnackbar"
                              
      HorizontalAlignment="Center"
                     VerticalAlignment="Bottom"
                     MessageQueue="{materialDesign:MessageQueue}" />
    </Grid>
</Window>
